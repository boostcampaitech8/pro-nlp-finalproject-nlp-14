# Stateless DB를 위한 클러스터 외부 디비 서버
# dev 환경에서는 env 설정을 할 필요 없습니다.

# DB
POSTGRES_PASSWORD=mitpassword
NEO4J_PASSWORD=mitpassword

# pgAdmin (profile: pgadmin)
PGADMIN_EMAIL=admin@mit.local
PGADMIN_PASSWORD=mitpassword

# Neo4j Bolt advertised host (프로덕션 터널용)
NEO4J_BOLT_HOST=localhost

# Cloudflare Tunnel (profile: cloudflare)
CLOUDFLARE_TUNNEL_TOKEN=
