# ===================
# Production Settings
# ===================
# Docker 통합 배포시 nginx가 /api를 백엔드로 프록시하므로 상대 경로 사용
VITE_API_URL=/api/v1

# 배포:
# docker compose -f docker/docker-compose.yml up -d
# Frontend + Backend 통합: http://localhost:3000
