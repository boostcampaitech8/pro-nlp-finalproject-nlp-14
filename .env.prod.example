# k8s 프로덕션 서버용 .env 예시
# - 앱 비밀값 SSOT: GCP Secret Manager -> ESO -> K8s Secret(mit-secrets)
# - 이 파일은 서버 로컬/운영 스크립트용 비비밀값만 관리

# LiveKit 네트워크 설정용 (deploy-prod.sh --setup-network)
LB_EXTERNAL_IP=

# K3s HA Cluster (3 server, taint 제거)
NODE1=        # 노드1 사설IP (초기 server, LiveKit 고정)
NODE2=        # 노드2 사설IP (server, --server https://NODE1:6443 으로 조인)
NODE3=        # 노드3 사설IP (server, --server https://NODE1:6443 으로 조인)
K3S_TOKEN=    # openssl rand -hex 32

# 참고: mit-secrets에 필요한 주요 key 예시
# DATABASE_URL, JWT_SECRET_KEY, keys.yaml, LIVEKIT_API_KEY, LIVEKIT_API_SECRET
# NEO4J_USER, NEO4J_PASSWORD, CLOVA_STT_SECRET_0..4
# BACKEND_API_KEY, TTS_SERVER_URL, NCP_CLOVASTUDIO_API_KEY
# CLOVA_ROUTER_ID, CLOVA_ROUTER_VERSION
# LANGFUSE_SECRET_KEY, LANGFUSE_PUBLIC_KEY, LANGFUSE_BASE_URL, LANGFUSE_TRACING_ENABLED
# GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI
# NAVER_CLIENT_ID, NAVER_CLIENT_SECRET, NAVER_REDIRECT_URI
# GRAFANA_ADMIN_USER, GRAFANA_ADMIN_PASSWORD, CLOUDFLARE_TUNNEL_TOKEN
